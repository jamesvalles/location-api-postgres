package mainapp.controller;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class LoggerInstance {
  protected final static Logger LOG = LoggerFactory.getLogger(LoggerInstance.class);
}
